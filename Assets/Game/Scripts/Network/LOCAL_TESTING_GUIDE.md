# 🖥️ Тестирование мультиплеера локально (один компьютер)

## ✅ Да, можно подключиться с одного ПК в двух браузерах/окнах!

Photon PUN 2 поддерживает несколько подключений с одного IP-адреса, так что вы можете тестировать мультиплеер локально.

---

## 🌐 Вариант 1: Тестирование в браузере (WebGL билд)

### Преимущества:
- ✅ Самый простой способ
- ✅ Не нужно устанавливать дополнительное ПО
- ✅ Можно быстро открыть несколько вкладок/окон браузера

### Как сделать:

1. **Создайте WebGL билд:**
   - **File → Build Settings**
   - Выберите платформу: **WebGL**
   - Нажмите **"Switch Platform"** (если нужно)
   - Нажмите **"Build"**
   - Сохраните билд в папку (например, `Build/WebGL/`)

2. **Запустите билд локально:**
   - Используйте локальный веб-сервер (важно!):
     - **Простой способ:** Откройте папку билда в **VS Code** с расширением **Live Server**
     - **Или:** Используйте Python: `python -m http.server 8000` в папке билда
     - **Или:** Используйте любой другой локальный веб-сервер
   - ⚠️ **НЕ открывайте HTML файл напрямую** (`file://`) - нужен веб-сервер!

3. **Откройте в двух браузерах/окнах:**
   - Откройте URL (например, `http://localhost:8000`) в **первом браузере**
   - Откройте тот же URL в **втором браузере** (или новом окне)
   - Оба экземпляра подключатся к Photon как отдельные игроки!

4. **Проверка:**
   - Оба игрока должны увидеть друг друга
   - Управление каждого игрока должно работать независимо

### ⚠️ Важные замечания для WebGL:
- Обязательно используйте **локальный веб-сервер** (не открывайте файл напрямую)
- Photon WebGL использует WebSocket соединения
- Каждый браузер/вкладка считается отдельным игроком

---

## 🖥️ Вариант 2: Standalone билд (запуск нескольких экземпляров)

### Как сделать:

1. **Создайте Standalone билд:**
   - **File → Build Settings**
   - Выберите платформу: **Windows/Mac/Linux**
   - Нажмите **"Build"**
   - Сохраните билд в папку

2. **Запустите несколько экземпляров:**
   - **Windows:**
     - Запустите первый экземпляр: `TankGame.exe`
     - Запустите второй экземпляр: еще раз `TankGame.exe` (или создайте ярлык)
   - **Mac:**
     - Запустите первый экземпляр: откройте `.app` файл
     - Запустите второй: скопируйте `.app` файл и откройте копию
   - **Linux:**
     - Запустите первый: `./TankGame`
     - В другом терминале: `./TankGame` (из другой папки или скопированный билд)

3. **Проверка:**
   - Оба окна должны подключиться к Photon
   - Каждое окно - отдельный игрок

### ⚠️ Возможные проблемы:

#### Проблема 1: Порт занят
**Симптомы:** Второй экземпляр не запускается или выдает ошибку порта

**Решение:**
- Photon использует облачные серверы, так что порты обычно не проблема
- Если проблема с портами Unity (для локальной отладки), можно использовать разные папки для билдов

#### Проблема 1: Один экземпляр захватывает ввод
**Симптомы:** Ввод работает только в одном окне

**Решение:**
- Это нормально - фокус окна определяет, какой экземпляр получает ввод
- Переключайтесь между окнами, чтобы тестировать каждого игрока
- Для полноценного тестирования лучше использовать два компьютера

---

## 🎮 Вариант 3: Тестирование в Unity Editor + Билд

### Как сделать:

1. **Откройте игру в Unity Editor:**
   - Нажмите **Play** в Unity

2. **Запустите Standalone билд:**
   - Запустите билд игры на том же компьютере
   - Или откройте WebGL билд в браузере

3. **Проверка:**
   - Unity Editor и билд должны подключиться к одной комнате
   - Каждый - отдельный игрок

### ⚠️ Замечания:
- Unity Editor считается отдельным игроком
- Это удобно для быстрого тестирования без создания билда

---

## 🔧 Рекомендации для локального тестирования

### 1. **Установите Dev Region в Photon Settings:**
   - **Photon → Pun → Wizard**
   - Установите **Dev Region**: `eu` или `us` (ближайший к вам)
   - Это гарантирует, что оба клиента подключаются к одному региону

### 2. **Используйте фиксированное имя комнаты:**
   - В `PhotonNetworkManager` установите `roomName = "TestRoom"`
   - Это гарантирует, что оба клиента попадут в одну комнату

### 3. **Включите детальное логирование:**
   - В **Photon Server Settings** установите **Pun Logging**: `Full` (для отладки)
   - Проверяйте консоль на наличие сообщений о подключении

### 4. **Проверьте настройки PhotonView:**
   - Убедитесь, что префаб танка имеет компонент `PhotonView`
   - `PhotonView` должен быть настроен на синхронизацию

---

## 📋 Чеклист для локального тестирования

### Перед запуском:
- [ ] Photon App ID настроен
- [ ] Dev Region установлен в Photon Settings
- [ ] Room Name установлен в PhotonNetworkManager
- [ ] Префаб танка в `Assets/Resources/`
- [ ] PhotonView настроен на префабе танка

### При запуске двух клиентов:
- [ ] Первый клиент подключился: `[PhotonNetworkManager] Connected to Photon`
- [ ] Первый клиент в комнате: `[PhotonNetworkManager] Joined room`
- [ ] Первый танк заспавнен: `[PhotonNetworkManager] Player tank spawned`
- [ ] Второй клиент подключился
- [ ] Второй клиент в комнате
- [ ] Второй танк заспавнен
- [ ] Оба танка видны друг другу

---

## 🐛 Решение проблем

### Проблема: "Два клиента не видят друг друга"

**Причины и решения:**

1. **Разные комнаты:**
   - Убедитесь, что `roomName` одинаковый в обоих клиентах
   - Проверьте логи: `[PhotonNetworkManager] Joined room: MainRoom` (должно быть одинаково)

2. **Префаб не в Resources:**
   - Префаб танка ДОЛЖЕН быть в `Assets/Resources/`
   - Имя префаба должно совпадать с тем, что в коде

3. **PhotonView не настроен:**
   - Убедитесь, что на префабе танка есть `PhotonView`
   - `PhotonView` должен иметь `TankNetworkPhoton` в `Observed Components`

4. **Разные версии игры:**
   - Убедитесь, что оба клиента используют одну и ту же версию билда
   - `App Version` в Photon Settings должен быть одинаковым (или оба пустыми)

### Проблема: "Второй клиент не может подключиться"

**Решения:**
- Проверьте интернет-соединение
- Проверьте логи второго клиента на ошибки
- Убедитесь, что Photon App ID правильный в обоих клиентах

---

## ✅ Быстрый тест (минимальный чеклист)

1. **WebGL билд:**
   - Создайте WebGL билд
   - Запустите через локальный веб-сервер
   - Откройте в двух браузерах
   - Оба должны подключиться к Photon
   - Оба должны войти в одну комнату
   - Оба должны увидеть танки друг друга

2. **Проверка логов:**
   - В каждом клиенте должны быть сообщения:
     - `[PhotonNetworkManager] Connected to Photon Master Server`
     - `[PhotonNetworkManager] Joined room: MainRoom`
     - `[PhotonNetworkManager] Player tank spawned`

---

## 💡 Советы

### Для лучшего тестирования:
- Используйте **WebGL + два браузера** - самый простой способ
- Для тестирования управления используйте **два компьютера** (если возможно)
- Используйте **фиксированное имя комнаты** для гарантии, что клиенты попадут в одну комнату

### Для отладки:
- Включите **Pun Logging: Full** в Photon Settings
- Проверяйте консоль Unity / консоль браузера (F12)
- Используйте `PhotonDiagnostics` компонент для визуальной диагностики

---

## 🎯 Итог

**Да, можно подключиться с одного ПК в двух браузерах!**

**Рекомендуемый способ:**
1. Создайте WebGL билд
2. Запустите через локальный веб-сервер (VS Code Live Server или Python http.server)
3. Откройте в двух браузерах/окнах
4. Оба клиента подключатся к Photon как отдельные игроки

**Помните:**
- Photon поддерживает несколько подключений с одного IP
- Каждый браузер/вкладка = отдельный игрок
- Используйте фиксированное имя комнаты для гарантии соединения


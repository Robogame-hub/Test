# 🌐 Photon: Unity Editor vs Браузер (WebGL билд)

## 📋 Краткий ответ:

**Комната Photon создается НЕ локально!** Она создается на серверах Photon в облаке. И Unity Editor, и браузер подключаются к одним и тем же серверам Photon.

---

## ✅ Как работает Photon:

### 1. **Комната создается на серверах Photon** (в облаке)
   - ❌ НЕ на вашем компьютере
   - ❌ НЕ в Unity Editor
   - ❌ НЕ в браузере
   - ✅ На серверах Photon в интернете

### 2. **Unity Editor и браузер подключаются к ОДНИМ серверам**
   - Unity Editor → Photon серверы → комната
   - Браузер → Photon серверы → та же комната
   - Если оба используют одинаковые настройки (App ID, Room Name) → попадают в одну комнату

---

## 🎮 Где можно тестировать:

### Вариант 1: **Только Unity Editor** (самый простой)
   - ✅ Запустите игру в Unity (Play)
   - ✅ Photon подключится к серверам
   - ✅ Создаст/присоединится к комнате
   - ⚠️ **НО:** вы не увидите других игроков (один клиент = один игрок)

### Вариант 2: **Unity Editor + Браузер** (рекомендуется)
   - ✅ Unity Editor: запустите игру (Play)
   - ✅ Браузер: запустите WebGL билд
   - ✅ Оба подключатся к Photon серверам
   - ✅ Оба попадут в одну комнату (если `roomName` одинаковый)
   - ✅ Вы увидите друг друга!

### Вариант 3: **Два браузера** (полный тест)
   - ✅ Браузер 1: запустите WebGL билд
   - ✅ Браузер 2: запустите тот же WebGL билд
   - ✅ Оба подключатся к Photon серверам
   - ✅ Оба попадут в одну комнату
   - ✅ Вы увидите друг друга!

---

## 🔧 Настройки для подключения к одной комнате:

### 1. **App ID должен быть одинаковым**
   - Unity Editor: App ID из `PhotonServerSettings`
   - WebGL билд: App ID из `PhotonServerSettings` (тот же файл!)
   - ✅ Если одинаковый → подключаются к одним серверам

### 2. **Room Name должен быть одинаковым**
   - Unity Editor: `roomName = "MainRoom"` в `PhotonNetworkManager`
   - WebGL билд: `roomName = "MainRoom"` (тот же в коде!)
   - ✅ Если одинаковый → попадают в одну комнату

### 3. **Game Version должен быть одинаковым** (опционально)
   - Unity Editor: `gameVersion = "1.0"`
   - WebGL билд: `gameVersion = "1.0"` (тот же в коде!)
   - ✅ Если одинаковый → не разделятся по версиям

---

## 🧪 Как проверить, что работает:

### Тест 1: Проверьте подключение в Unity Editor

1. Запустите игру в Unity (Play)
2. Откройте консоль (Console)
3. Проверьте логи:
   ```
   [PhotonNetworkManager] ✓ OnConnectedToMaster() callback called!
   [PhotonNetworkManager] ✓ OnJoinedRoom() callback called! Joined room: MainRoom
   [PhotonNetworkManager] Room info - Players: 1/16
   ```
4. ✅ Если видите эти логи → Photon подключен в Unity Editor

### Тест 2: Проверьте подключение в браузере

1. Создайте WebGL билд:
   - **File → Build Settings**
   - Выберите **WebGL**
   - Нажмите **"Build"**

2. Запустите билд через локальный веб-сервер:
   - VS Code Live Server
   - Python: `python -m http.server 8000`
   - Или другой локальный сервер

3. Откройте в браузере (F12 для консоли)
4. Проверьте логи в консоли браузера:
   ```
   [PhotonNetworkManager] ✓ OnJoinedRoom() callback called! Joined room: MainRoom
   [PhotonNetworkManager] Room info - Players: 2/16  ← Должно быть 2 игрока!
   ```
5. ✅ Если видите `Players: 2/16` → оба клиента в одной комнате!

---

## ⚠️ Важные замечания:

### 1. **WebGL требует локальный веб-сервер**
   - ❌ Не открывайте HTML файл напрямую (`file://`)
   - ✅ Используйте локальный веб-сервер (VS Code Live Server, Python http.server)
   - **Почему:** WebGL использует WebSocket для Photon, который не работает с `file://`

### 2. **App ID должен быть одинаковым**
   - Unity Editor и WebGL билд используют один и тот же файл `PhotonServerSettings.asset`
   - ✅ При создании билда App ID автоматически копируется в билд
   - ✅ Не нужно настраивать отдельно для браузера

### 3. **Комната создается первым игроком**
   - Первый клиент (Unity Editor или браузер) создает комнату
   - Второй клиент присоединяется к существующей комнате
   - ✅ Это работает автоматически через `JoinOrCreateRoom()`

---

## 📊 Диаграмма подключения:

```
Unity Editor                    Браузер
    |                             |
    |                             |
    v                             v
    |                             |
    +-----------------------------+
                |
                v
         Photon Cloud Servers
                |
                v
          Room: "MainRoom"
         (Players: 2/16)
```

**Оба клиента подключаются к одним серверам Photon!**

---

## 🔍 Отладка:

### Проблема: Unity Editor подключился, но браузер не видит

**Проверьте:**
1. ✅ Одинаковый App ID (в `PhotonServerSettings`)
2. ✅ Одинаковый Room Name (`MainRoom`)
3. ✅ Браузер использует локальный веб-сервер (не `file://`)
4. ✅ Интернет-соединение работает

### Проблема: Разные комнаты

**Причина:** Разные `roomName` или `gameVersion`

**Решение:**
- Убедитесь, что `roomName = "MainRoom"` в `PhotonNetworkManager`
- Убедитесь, что `gameVersion = "1.0"` одинаковый

### Проблема: Браузер не подключается к Photon

**Причина:** WebGL блокирует WebSocket с `file://`

**Решение:**
- Используйте локальный веб-сервер
- НЕ открывайте HTML напрямую

---

## ✅ Итог:

### Где создается комната:
- ❌ НЕ локально на вашем ПК
- ❌ НЕ в Unity Editor
- ❌ НЕ в браузере
- ✅ **На серверах Photon в облаке**

### Где можно тестировать:
- ✅ **Unity Editor** - для проверки подключения
- ✅ **Unity Editor + Браузер** - для тестирования мультиплеера
- ✅ **Два браузера** - для полного теста

### Главное:
- **App ID** должен быть одинаковым → подключаются к одним серверам
- **Room Name** должен быть одинаковым → попадают в одну комнату
- **WebGL билд** должен запускаться через локальный веб-сервер (не `file://`)

---

## 🎯 Рекомендация:

**Для тестирования мультиплеера:**

1. **Unity Editor** - проверьте подключение к Photon
2. **WebGL билд в браузере** - для тестирования с другими игроками
3. **Оба одновременно** - для тестирования мультиплеера на одном ПК

**Итог:** Комната создается на серверах Photon, а не локально. Unity Editor и браузер подключаются к одним серверам, если используют одинаковые настройки!


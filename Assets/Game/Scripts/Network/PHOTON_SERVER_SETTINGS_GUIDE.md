# 📋 Инструкция по настройке Photon Server Settings

## 🎯 Как открыть Photon Server Settings

### Способ 1: Через меню Unity
1. Откройте **Photon → Pun → Wizard** в меню Unity
2. В окне Wizard нажмите кнопку **"Show Settings"** или **"Edit Settings"**

### Способ 2: Через Project окно
1. Откройте **Project** окно в Unity
2. Найдите файл: `Assets/Photon/PhotonUnityNetworking/Resources/PhotonServerSettings`
3. Выберите файл - настройки отобразятся в **Inspector**

---

## ✅ Обязательные настройки (минимум для работы)

### 1. **App ID Realtime** ⚠️ КРИТИЧЕСКИ ВАЖНО

**Что это:** Идентификатор вашего приложения Photon

**Как настроить:**
1. Зайдите на https://dashboard.photonengine.com/
2. Войдите в свой аккаунт (или создайте новый)
3. Создайте новое приложение (если еще не создано):
   - Нажмите **"Create a New App"**
   - Выберите тип: **"Photon PUN"** или **"Photon Realtime"**
   - Введите название (например, "Tank Game")
   - Выберите регион (ближайший к вам)
   - Нажмите **"Create"**
4. Скопируйте **App ID** из панели приложения
5. В Unity: **Photon → Pun → Wizard** → Вставьте App ID в поле **"App ID Realtime"**
6. Нажмите **"Setup Project"**

**Текущее значение:** `80f5435e-1e1b-42a2-a595-25539da7d0ec` ✅ (настроено)

**Проверка:** Должно быть заполнено (не пустое)

---

## ⚙️ Рекомендуемые настройки

### 2. **App Version** (опционально, но рекомендуется)

**Что это:** Версия вашего приложения (для разделения игроков разных версий)

**Как настроить:**
- В **Photon → Pun → Wizard** → поле **"App Version"**
- Например: `1.0` или `1.0.0`
- Если пусто, все версии будут совместимы

**Рекомендация:** Оставьте пустым для тестирования, укажите версию для релиза

---

### 3. **Use Name Server** ✅

**Что это:** Использовать Photon Name Server для автоматического выбора региона

**Текущее значение:** `1` (включено) ✅

**Рекомендация:** Оставьте включенным (`1` или `☑`)

---

### 4. **Fixed Region** (опционально)

**Что это:** Фиксированный регион для подключения (если не хотите использовать автоматический выбор)

**Текущее значение:** Пусто (используется автоматический выбор) ✅

**Варианты:** `eu`, `us`, `asia`, `sa`, `jp`, `au`, `kr`, `in`, `ru`, `usw`, `cae`, `cn`

**Рекомендация:** 
- Для тестирования можно указать `eu` или `us`
- Для продакшена оставьте пустым (автоматический выбор лучшего региона)

---

### 5. **Dev Region** (для разработки)

**Что это:** Регион, используемый в Editor и Development билдах

**Текущее значение:** `eu` ✅

**Рекомендация:** Установите регион, ближайший к вам (для быстрого тестирования)

**Варианты:** `eu`, `us`, `asia`, `sa`, `jp`, `au`, `kr`, `in`, `ru`, `usw`, `cae`, `cn`

---

### 6. **Start In Offline Mode** ⚠️

**Что это:** Запускать Photon в офлайн-режиме (без подключения к серверу)

**Текущее значение:** `0` (выключено) ✅

**Рекомендация:** **ВСЕГДА** должно быть выключено (`0` или `☐`) для онлайн-игры!

---

### 7. **Run In Background** ✅

**Что это:** Продолжать работу Photon, когда приложение теряет фокус

**Текущее значение:** `1` (включено) ✅

**Рекомендация:** Оставьте включенным (`1` или `☑`)

---

## 🔍 Дополнительные настройки (для отладки)

### 8. **Pun Logging**

**Что это:** Уровень логирования Photon PUN

**Текущее значение:** `0` (ErrorsOnly - только ошибки)

**Варианты:**
- `0` = ErrorsOnly (только ошибки) - минимальное логирование
- `1` = Full (полное логирование) - для отладки
- `2` = Informational (информационные сообщения)

**Рекомендация:** 
- Для тестирования: установите `1` (Full) для детальных логов
- Для продакшена: оставьте `0` (ErrorsOnly)

**Как изменить:** В **Photon → Pun → Wizard** → **"Pun Logging"** → выберите нужный уровень

---

### 9. **Network Logging**

**Что это:** Логирование сетевого трафика Photon

**Текущее значение:** `1` (включено) ✅

**Рекомендация:** Оставьте включенным для диагностики проблем

---

### 10. **Enable Support Logger** (опционально)

**Что это:** Включить дополнительное логирование для поддержки Photon

**Текущее значение:** `0` (выключено)

**Рекомендация:** Включите (`1` или `☑`), если есть проблемы с подключением

---

## 🚫 Настройки, которые обычно НЕ нужно менять

### **Protocol** (по умолчанию 0 - UDP)
- Оставьте `0` (UDP) для лучшей производительности
- `1` (TCP) только если UDP заблокирован

### **Port** (по умолчанию 0 - автоматический)
- Оставьте `0` для автоматического выбора порта
- Меняйте только если есть проблемы с файрволом

### **Proxy Server** (по умолчанию пусто)
- Оставьте пустым, если не используете прокси-сервер

---

## ✅ Чеклист настройки

Перед запуском игры проверьте:

- [ ] **App ID Realtime** заполнен и правильный
- [ ] **Start In Offline Mode** выключено (`☐`)
- [ ] **Use Name Server** включено (`☑`)
- [ ] **Run In Background** включено (`☑`)
- [ ] **Dev Region** установлен (например, `eu`)

---

## 🔧 Как проверить, что настройки правильные

### Способ 1: Через Photon Wizard
1. **Photon → Pun → Wizard**
2. Проверьте, что **App ID Realtime** заполнен
3. Нажмите **"Check Setup"** (если есть такая кнопка)

### Способ 2: Запустить игру и проверить логи
1. Запустите игру в Unity (Play)
2. Откройте **Console** (Window → General → Console)
3. Ищите сообщения:
   - ✅ `[PhotonNetworkManager] Connected to Photon Master Server` - подключение успешно
   - ✅ `[PhotonNetworkManager] Joined room: MainRoom` - вход в комнату успешен
   - ❌ `App ID not set` или `Not connected` - проблема с настройками

### Способ 3: Использовать PhotonDiagnostics
1. Добавьте компонент `PhotonDiagnostics` в сцену
2. Запустите игру - появится диагностическая панель на экране
3. Проверьте статус подключения

---

## ❌ Частые ошибки

### Ошибка 1: "App ID not set"
**Решение:** Откройте **Photon → Pun → Wizard** и заполните **App ID Realtime**

### Ошибка 2: "Not connected to Photon"
**Решение:** 
- Проверьте интернет-соединение
- Проверьте, что **Start In Offline Mode** выключено
- Проверьте, что App ID правильный

### Ошибка 3: "Cannot connect to region"
**Решение:**
- Установите **Fixed Region** или **Dev Region** (например, `eu`)
- Или оставьте пустым для автоматического выбора

---

## 📚 Дополнительная информация

### Полезные ссылки:
- **Photon Dashboard**: https://dashboard.photonengine.com/
- **Photon PUN 2 Documentation**: https://doc.photonengine.com/pun/current
- **Photon Regions**: https://doc.photonengine.com/pun/current/connection-and-authentication/regions

### Текущие настройки вашего проекта:

```
AppIdRealtime: 80f5435e-1e1b-42a2-a595-25539da7d0ec ✅
UseNameServer: 1 ✅
FixedRegion: (пусто - автоматический выбор) ✅
DevRegion: eu ✅
StartInOfflineMode: 0 ✅ (выключено - хорошо!)
RunInBackground: 1 ✅
PunLogging: 0 (ErrorsOnly)
NetworkLogging: 1 ✅
```

**Вывод:** Основные настройки выглядят правильно! Если онлайн не работает, проблема, скорее всего, не в Photon Server Settings.

---

## 🎯 Что сделать дальше, если онлайн не работает:

1. Проверьте логи в консоли Unity при запуске игры
2. Убедитесь, что PhotonNetworkManager есть в сцене и настроен
3. Проверьте, что префаб танка находится в `Assets/Resources/`
4. Проверьте, что на префабе танка есть компонент `PhotonView`
5. Убедитесь, что оба игрока используют одну и ту же версию игры и один и тот же App ID


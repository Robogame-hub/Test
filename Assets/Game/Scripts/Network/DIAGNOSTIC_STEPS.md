# 🔍 Пошаговая диагностика: Нет подключения к комнате в Unity Editor

## ❌ Проблема:
Диагностика показывает: "Connected: ✗ NO" и "In Room: ✗ NO"

---

## 📋 Шаг 1: Проверьте логи в консоли Unity

Откройте **Console** (Window → General → Console) и проверьте, есть ли логи:

### ✅ Должны быть эти логи при запуске:

```
[PhotonNetworkManager] Start() called! GameObject: PhotonNetworkManager, Active: True, autoConnectOnStart: True
[PhotonNetworkManager] Start(): Not connected, calling Connect()...
[PhotonNetworkManager] Calling PhotonNetwork.ConnectUsingSettings()...
[PhotonNetworkManager] ✓ OnConnectedToMaster() callback called!
[PhotonNetworkManager] Attempting to join/create room: 'MainRoom'
[PhotonNetworkManager] ✓ JoinOrCreateRoom() called successfully!
[PhotonNetworkManager] ✓ OnJoinedRoom() callback called! Joined room: MainRoom
```

### ❌ Если НЕТ логов `[PhotonNetworkManager]`:

**Проблема:** PhotonNetworkManager отсутствует в сцене или неактивен

**Решение:**
1. Откройте сцену `Assets/Scenes/Core.unity`
2. В **Hierarchy** найдите `PhotonNetworkManager`
3. Если НЕТ - создайте:
   - **GameObject → Create Empty**
   - Назовите `PhotonNetworkManager`
   - **Add Component → PhotonNetworkManager (TankGame.Network)**

### ❌ Если есть лог: `autoConnectOnStart is FALSE`:

**Проблема:** Auto Connect выключен

**Решение:**
1. Выберите `PhotonNetworkManager` в Hierarchy
2. В **Inspector** включите **Auto Connect On Start**: `☑`

### ❌ Если есть ошибка: `Photon App ID not configured!`:

**Проблема:** App ID не настроен

**Решение:**
1. **Photon → Pun → Wizard**
2. Заполните **App ID Realtime**: `80f5435e-1e1b-42a2-a595-25539da7d0ec`
3. Нажмите **Setup Project**

### ❌ Если есть лог `ConnectUsingSettings() called`, но НЕТ `OnConnectedToMaster()`:

**Проблема:** Photon не может подключиться

**Возможные причины:**
- Интернет-соединение не работает
- Брандмауэр блокирует Photon
- Start In Offline Mode включен

**Решение:**
1. Проверьте интернет-соединение
2. **Photon → Pun → Wizard → Show Settings**
3. Проверьте **Start In Offline Mode**: должен быть `☐` (выключено)

### ❌ Если есть `OnConnectedToMaster()`, но НЕТ `OnJoinedRoom()`:

**Проблема:** Не может присоединиться к комнате

**Проверьте логи:**
- Есть ли лог `JoinOrCreateRoom() called successfully`?
- Есть ли ошибка `JoinOrCreateRoom() returned FALSE`?

**Решение:**
- Photon автоматически повторит попытку через 1 секунду
- Проверьте, что `Room Name` заполнен в `PhotonNetworkManager`

---

## 📋 Шаг 2: Проверьте настройки PhotonNetworkManager

1. Выберите `PhotonNetworkManager` в **Hierarchy**
2. Проверьте в **Inspector**:

- [ ] **Auto Connect On Start**: `☑` (включено)
- [ ] **Room Name**: `MainRoom` (заполнено, не пустое!)
- [ ] **Max Players Per Room**: любое число (например, `16`)
- [ ] **Tank Prefab**: назначен (префаб из `Resources/TANK`)

---

## 📋 Шаг 3: Проверьте Photon Server Settings

1. **Photon → Pun → Wizard**
2. Нажмите **Show Settings** (или **Edit Settings**)
3. Проверьте:

- [ ] **App ID Realtime**: `80f5435e-1e1b-42a2-a595-25539da7d0ec` (заполнен)
- [ ] **Start In Offline Mode**: `☐` (выключено!)
- [ ] **Dev Region**: `eu` (или другой регион)

---

## 📋 Шаг 4: Проверьте PhotonDiagnostics

1. В сцене должен быть GameObject с компонентом `PhotonDiagnostics`
2. Запустите игру (Play)
3. Проверьте окно диагностики на экране (справа вверху)

**Должно быть:**
- **Status**: `✓ CONNECTED` (зеленый)
- **In Room**: `✓ YES - MainRoom` (зеленый)
- **Players**: `1/16` (или больше)

**Если красное:**
- `✗ DISCONNECTED` → Photon не подключен
- `In Room: ✗ NO` → Не в комнате

---

## 🧪 Быстрый тест:

### Тест 1: Проверьте, что PhotonNetworkManager вызывается

1. Запустите игру (Play)
2. В консоли должен быть лог:
   ```
   [PhotonNetworkManager] Start() called! ...
   ```
3. Если НЕТ → `PhotonNetworkManager` отсутствует или неактивен

### Тест 2: Проверьте подключение

1. Запустите игру (Play)
2. В консоли должен быть лог:
   ```
   [PhotonNetworkManager] ✓ OnConnectedToMaster() callback called!
   ```
3. Если НЕТ → Photon не подключается (проверьте App ID и интернет)

### Тест 3: Проверьте вход в комнату

1. Запустите игру (Play)
2. В консоли должен быть лог:
   ```
   [PhotonNetworkManager] ✓ OnJoinedRoom() callback called! Joined room: MainRoom
   ```
3. Если НЕТ → Не может войти в комнату (проверьте логи ошибок)

---

## ✅ Итоговый чеклист:

Перед запуском убедитесь:

- [ ] PhotonNetworkManager в сцене (GameObject в Hierarchy)
- [ ] **Auto Connect On Start**: `☑` (включено)
- [ ] **Room Name**: `MainRoom` (заполнено)
- [ ] **App ID** настроен в Photon Settings
- [ ] **Start In Offline Mode**: `☐` (выключено)
- [ ] Интернет-соединение работает

После этого запустите игру и проверьте логи в консоли!

---

## 💡 Если ничего не помогает:

1. **Включите детальное логирование Photon:**
   - **Photon → Pun → Wizard → Show Settings**
   - **Pun Logging**: `Full` (вместо `ErrorsOnly`)
   - Перезапустите игру

2. **Проверьте консоль на ошибки:**
   - Могут быть красные ошибки от Photon
   - Они покажут причину проблемы

3. **Попробуйте подключиться вручную:**
   - В коде временно добавьте вызов `PhotonNetworkManager.Instance.Connect()` из другого скрипта
   - Проверьте, вызывается ли он

---

## 📞 Что сообщить для помощи:

Если проблема не решается, пришлите:
1. Все логи из консоли при запуске игры (первые 20-30 строк)
2. Скриншот Inspector `PhotonNetworkManager`
3. Результаты `PhotonDiagnostics` на экране

Это поможет быстро найти проблему!


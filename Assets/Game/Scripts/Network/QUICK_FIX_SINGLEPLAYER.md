# 🔧 Быстрое исправление: Игра запускается как сингл-плеер для каждого игрока

## ✅ Что было исправлено:

1. **Fallback спавн** - теперь НЕ спавнит локальный танк, когда Photon подключен
2. **Проверка подключения** - танки спавнятся через Photon только когда подключены к комнате
3. **Диагностика** - добавлены логи для проверки подключения и спавна танков

---

## 📋 Чеклист: Убедитесь что все настроено правильно

### 1. PhotonNetworkManager в сцене:

- [ ] В сцене есть GameObject с компонентом `PhotonNetworkManager`
- [ ] **Room Name**: `MainRoom` (или другое фиксированное имя) ✅
- [ ] **Auto Connect On Start**: `☑` (включено) ✅
- [ ] **Tank Prefab**: назначен префаб из `Resources/TANK` ✅
- [ ] **Fallback Spawn Delay**: `5` или `0` (0 = отключить fallback) ✅

### 2. Префаб танка (TANK.prefab):

- [ ] Префаб находится в `Assets/Resources/TANK.prefab` ✅
- [ ] На префабе есть компонент `PhotonView` ✅
- [ ] В `PhotonView` → **Observed Components** есть `TankNetworkPhoton` ✅
- [ ] На префабе есть компонент `TankNetworkPhoton` ✅

### 3. Photon Server Settings:

- [ ] **App ID Realtime** заполнен ✅
- [ ] **Start In Offline Mode**: `☐` (выключено) ✅
- [ ] **Dev Region**: установлен (например, `eu`) ✅

---

## 🧪 Как проверить что все работает:

### Шаг 1: Запустите игру в Unity

1. Откройте консоль Unity (**Window → General → Console**)
2. Нажмите **Play**
3. Проверьте логи - должны быть:

```
[PhotonNetworkManager] ✓ OnConnectedToMaster() callback called!
[PhotonNetworkManager] ✓ OnJoinedRoom() callback called! Joined room: MainRoom
[PhotonNetworkManager] Room info - Players: 1/16
[PhotonNetworkManager] Spawning player tank via Photon...
[PhotonNetworkManager] Player tank spawned via Photon: ...
```

### Шаг 2: Откройте второй клиент (в браузере или второй экземпляр)

1. Создайте WebGL билд или запустите второй экземпляр
2. Откройте консоль (F12 в браузере или логи билда)
3. Проверьте логи второго клиента:

```
[PhotonNetworkManager] ✓ OnJoinedRoom() callback called! Joined room: MainRoom
[PhotonNetworkManager] Room info - Players: 2/16  ← Должно быть 2 игрока!
[PhotonNetworkManager] Existing tanks in scene before spawn: 1  ← Должен видеть танк первого игрока!
```

### Шаг 3: Проверьте видимость танков

- **Первый игрок**: должен видеть свой танк
- **Второй игрок**: должен видеть СВОЙ танк И танк первого игрока
- Если второй игрок не видит танк первого - проблема с синхронизацией PhotonView

---

## ❌ Если игроки все еще не видят друг друга:

### Проблема 1: "Tank spawned via Photon" но другие игроки не видят

**Причина:** Префаб не в Resources или имя не совпадает

**Решение:**
1. Убедитесь, что префаб `TANK.prefab` в папке `Assets/Resources/`
2. В `PhotonNetworkManager` префаб должен быть назначен из Resources
3. Проверьте логи - должно быть: `GetPrefabResourcePath returning: 'TANK'`

### Проблема 2: "PhotonNetwork.Instantiate returned NULL"

**Причина:** Префаб не найден в Resources

**Решение:**
1. Переместите префаб в `Assets/Resources/`
2. Имя префаба должно быть `TANK` (без расширения)
3. Перезапустите игру

### Проблема 3: "Joined room: Room_XXXX" (разные имена комнат)

**Причина:** Имя комнаты не фиксировано

**Решение:**
1. В `PhotonNetworkManager` установите **Room Name**: `MainRoom`
2. Перезапустите оба клиента
3. Оба должны войти в `MainRoom`

### Проблема 4: Fallback спавнит локальный танк

**Причина:** Photon не подключился вовремя

**Решение:**
1. Проверьте интернет-соединение
2. Проверьте App ID в Photon Settings
3. Установите **Fallback Spawn Delay**: `0` (отключить fallback)

---

## 🔍 Дополнительная диагностика:

### Включите детальное логирование Photon:

1. **Photon → Pun → Wizard**
2. Установите **Pun Logging**: `Full`
3. Перезапустите игру

Это покажет все сетевые события в консоли.

### Проверьте PhotonView на заспавненных танках:

В консоли Unity в `OnJoinedRoom()` должны быть логи:
```
[PhotonNetworkManager] Existing tank: TANK(Clone), PhotonView.IsMine: False, Owner: Player1
```

Если `PhotonView` отсутствует или `IsMine` неправильно установлен - проблема в настройке префаба.

---

## ✅ Итоговый чеклист:

Перед запуском убедитесь:

- [ ] PhotonNetworkManager в сцене с правильными настройками
- [ ] Room Name = `MainRoom` (одинаковый для всех)
- [ ] Префаб танка в `Assets/Resources/TANK.prefab`
- [ ] PhotonView на префабе настроен правильно
- [ ] App ID в Photon Settings настроен
- [ ] Start In Offline Mode выключено

Если все отмечено, но проблема осталась - проверьте логи консоли на наличие ошибок!


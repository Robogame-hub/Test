# 🔧 Решение проблем с онлайн мультиплеером Photon

## ❌ Проблема: Не вижу других игроков / Онлайн не работает

### Шаг 1: Проверка установки Photon

1. **Установлен ли Photon PUN 2?**
   - Откройте **Window → Package Manager**
   - Проверьте наличие `com.photon.pun2` в списке пакетов
   - ❌ Если нет → Установите из Asset Store: **"PUN 2 - FREE"**

2. **Настроен ли App ID?**
   - Откройте **Photon → Pun → Wizard**
   - Проверьте, что **App ID Realtime** заполнен
   - ❌ Если пусто → Вставьте App ID из https://dashboard.photonengine.com/

### Шаг 2: Используйте диагностический скрипт

1. **Добавьте PhotonDiagnostics в сцену:**
   - Создайте пустой GameObject: **GameObject → Create Empty**
   - Назовите `PhotonDiagnostics`
   - Добавьте компонент: **Add Component → PhotonDiagnostics (TankGame.Network)**
   - Запустите игру - на экране появится диагностическая панель

2. **Проверьте консоль:**
   - ПКМ на компоненте `PhotonDiagnostics` → **"Check Photon Setup"**
   - Проверьте все сообщения в консоли

### Шаг 3: Проверка настроек

#### ✅ Чеклист настройки:

- [ ] **Photon PUN 2 установлен** (видно в Package Manager)
- [ ] **App ID настроен** (в Photon Wizard)
- [ ] **PhotonNetworkManager есть в сцене** (GameObject с компонентом `PhotonNetworkManager`)
- [ ] **Tank Prefab назначен в PhotonNetworkManager** (поле "Tank Prefab")
- [ ] **PhotonView на префабе танка** (компонент `PhotonView` на корневом GameObject префаба)
- [ ] **TankNetworkPhoton на префабе танка** (компонент `TankNetworkPhoton`)
- [ ] **Префаб танка в папке Resources** (`Assets/Resources/TANK.prefab`)

### Шаг 4: Типичные проблемы и решения

#### 🔴 Проблема 1: "Photon PUN 2 not installed"

**Симптомы:**
- В консоли: `[PhotonNetworkManager] Photon PUN 2 not installed!`
- В диагностике: `✗ PHOTON PUN 2 NOT INSTALLED`

**Решение:**
1. Откройте **Unity Asset Store**
2. Найдите **"PUN 2 - FREE"**
3. Нажмите **"Add to My Assets"** → **"Import"**
4. Перезапустите Unity

---

#### 🔴 Проблема 2: "App ID not set"

**Симптомы:**
- В консоли: ошибки подключения к Photon
- В диагностике: `App ID: ✗ NOT SET`

**Решение:**
1. Зайдите на https://dashboard.photonengine.com/
2. Создайте приложение (если еще не создано)
3. Скопируйте **App ID**
4. В Unity: **Photon → Pun → Wizard**
5. Вставьте **App ID** в поле **"App ID Realtime"**
6. Нажмите **"Setup Project"**

---

#### 🔴 Проблема 3: "Prefab not found in Resources"

**Симптомы:**
- При спавне: `PhotonNetwork.Instantiate failed`
- В диагностике: `Prefab in Resources: ✗ NO`

**Решение:**
1. Переместите префаб танка в папку `Resources`:
   - Перетащите `Assets/Game/Prefab/TANK.prefab` → `Assets/Resources/`
2. В **PhotonNetworkManager** обновите ссылку на префаб:
   - Перетащите префаб из `Resources` в поле **"Tank Prefab"**
3. **ВАЖНО:** Имя префаба должно совпадать с именем файла без расширения

---

#### 🔴 Проблема 4: "PhotonView is missing"

**Симптомы:**
- При спавне: `PhotonView is missing on GameObject`
- В диагностике: `PhotonView on prefab: ✗ NO`

**Решение:**
1. Откройте префаб танка: `Assets/Resources/TANK.prefab`
2. Выберите корневой GameObject танка
3. Добавьте компонент: **Add Component → Networking → Photon View**
4. Настройте **PhotonView**:
   - **Synchronization**: `Unreliable On Change`
   - **Observed Components**: Добавьте `TankNetworkPhoton` (перетащите компонент)

---

#### 🔴 Проблема 5: "TankNetworkPhoton is missing"

**Симптомы:**
- В диагностике: `TankNetworkPhoton on prefab: ✗ NO`

**Решение:**
1. Откройте префаб танка: `Assets/Resources/TANK.prefab`
2. Добавьте компонент: **Add Component → TankNetworkPhoton (TankGame.Network)**
3. Назначьте все ссылки:
   - **Tank Controller**: Перетащите `TankController`
   - **Input Handler**: Перетащите `TankInputHandler`
   - И т.д.

---

#### 🔴 Проблема 6: "Not connected to Photon"

**Симптомы:**
- В консоли: `Not connected to Photon!`
- В диагностике: `Status: ✗ DISCONNECTED`

**Решение:**
1. Проверьте интернет-соединение
2. Проверьте, что App ID правильный
3. Проверьте, что PhotonNetworkManager в сцене
4. Проверьте логи в консоли - там будет причина отключения

---

#### 🔴 Проблема 7: "Not in room"

**Симптомы:**
- В диагностике: `In Room: ✗ NO`

**Решение:**
1. PhotonNetworkManager должен автоматически подключиться при старте
2. Проверьте, что в `PhotonNetworkManager.Start()` вызывается `Connect()`
3. Проверьте логи - там будет причина ошибки подключения к комнате

---

#### 🔴 Проблема 8: "All tanks controlled by one player"

**Симптомы:**
- Все танки двигаются одновременно
- Ввод одного игрока влияет на всех

**Решение:**
1. Проверьте, что `photonView.IsMine` проверяется правильно в `TankNetworkPhoton`
2. Проверьте, что `SetIsLocalPlayer()` вызывается при спавне
3. Убедитесь, что каждый клиент спавнит свой танк через Photon

---

#### 🔴 Проблема 9: "Can't see other players"

**Симптомы:**
- Вижу только свой танк
- Другие игроки не появляются

**Решение:**
1. **Проверьте, что оба клиента в одной комнате:**
   - В диагностике проверьте имя комнаты - должно совпадать
2. **Проверьте спавн:**
   - Танки должны спавниться через `PhotonNetwork.Instantiate()`, а не `Instantiate()`
3. **Проверьте PhotonView:**
   - На каждом танке должен быть PhotonView
   - PhotonView должен быть настроен на синхронизацию
4. **Проверьте интерполяцию:**
   - В `TankNetworkPhoton` должно быть `enableInterpolation = true`

---

### Шаг 5: Тестирование

#### Локальное тестирование (один компьютер):

1. Запустите игру в Unity (Play)
2. Проверьте диагностику на экране
3. В консоли должны быть сообщения:
   - `[PhotonNetworkManager] Connected to Photon Master Server`
   - `[PhotonNetworkManager] Joined room: Room_XXXX`
   - `[PhotonNetworkManager] Player tank spawned: ...`

#### Онлайн тестирование (два компьютера):

1. **Первый компьютер:**
   - Запустите билд игры
   - Проверьте, что появился танк
   - Проверьте имя комнаты в диагностике

2. **Второй компьютер:**
   - Запустите тот же билд
   - Должен подключиться к той же комнате (если `roomName` одинаковый)
   - Должны увидеть танк первого игрока

3. **Если не работает:**
   - Проверьте, что оба компьютера в одной сети (или оба видят Photon серверы)
   - Проверьте логи на обоих компьютерах
   - Убедитесь, что `roomName` одинаковый (или оставьте пустым для случайного выбора)

---

## 📊 Дополнительная отладка

### Включение подробных логов Photon:

1. В `PhotonNetworkManager.Start()` добавьте:
```csharp
PhotonNetwork.LogLevel = PunLogLevel.Full;
```

2. Это покажет все сетевые события в консоли

### Проверка сетевого трафика:

1. В диагностике проверьте:
   - `PhotonViews in scene` - должно быть больше 0
   - `Local (Mine)` - должно быть минимум 1 (ваш танк)
   - `Remote (Others)` - должно быть количество других игроков

---

## ✅ Итоговый чеклист

Перед запуском проверьте:

- [ ] Photon PUN 2 установлен
- [ ] App ID настроен
- [ ] PhotonNetworkManager в сцене и настроен
- [ ] Префаб танка в `Resources`
- [ ] PhotonView на префабе танка
- [ ] TankNetworkPhoton на префабе танка
- [ ] Все ссылки назначены
- [ ] Диагностика показывает `✓ CONNECTED` и `In Room: ✓ YES`

Если все пункты отмечены, но не работает - проверьте логи консоли на наличие ошибок!

---

## 💡 Полезные ссылки

- **Photon Dashboard**: https://dashboard.photonengine.com/
- **Photon PUN 2 Docs**: https://doc.photonengine.com/pun/current
- **Photon Discord**: https://discord.gg/photon


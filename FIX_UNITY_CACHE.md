# 🔧 Исправление ошибки Unity кэша

## Проблема

Unity показывает ошибку:
```
error CS0103: The name 'suspensionOffset' does not exist in the current context
```

Но в коде этой переменной больше нет! Это проблема **кэша Unity**.

---

## ✅ Решение (выберите один способ)

### Способ 1: Assets → Reimport All (Самый простой) ⭐

1. В Unity: **Assets** → **Reimport All**
2. Подождите пока Unity переимпортирует все файлы
3. Ошибка должна исчезнуть

### Способ 2: Перезапуск Unity

1. **Сохраните сцену** (Ctrl+S)
2. **Закройте Unity полностью**
3. **Откройте проект заново**
4. Подождите импорт скриптов
5. Ошибка должна исчезнуть

### Способ 3: Очистка Library (Радикальный)

**⚠️ Внимание:** Unity будет долго переимпортировать все!

1. **Закройте Unity полностью**
2. Перейдите в папку проекта
3. **Удалите папку** `Library`
4. Откройте Unity
5. Подождите ~5-10 минут пока Unity создаст новую Library
6. Ошибка точно исчезнет

### Способ 4: Принудительная перекомпиляция

1. В Unity: **Edit** → **Preferences**
2. **External Tools** → **Regenerate project files**
3. Или просто добавьте пробел в любой .cs файл и сохраните
4. Unity перекомпилирует скрипты

---

## 🔍 Проверка что проблема решена

После любого из способов:

1. Откройте Console (Ctrl+Shift+C)
2. Проверьте что ошибки нет
3. Выберите танк в Hierarchy
4. Проверьте TankMovement в Inspector
5. Должны быть видны новые поля:
   - Suspension Points (4 поля)
   - Auto Create Points

---

## 📋 Если ошибка осталась

### Проверьте что файл сохранен

1. Откройте `TankMovement.cs` в VS Code/Rider
2. Проверьте что в коде нет `suspensionOffset`
3. Если есть - сохраните файл (Ctrl+S)

### Проверьте что Unity видит изменения

1. В Unity перейдите к файлу:
   `Assets/Game/Scripts/Tank/Components/TankMovement.cs`
2. Двойной клик - откроется редактор
3. Проверьте что код новый (есть `autoPointOffset`)

### Крайний случай - ручное исправление

Если ничего не помогает:

1. В Unity найдите TankMovement.cs
2. Правый клик → Delete
3. Создайте новый скрипт с тем же именем
4. Скопируйте код из файла

---

## ✅ Что должно быть в коде

В `TankMovement.cs` должны быть эти поля (строки 24-36):

```csharp
[Header("Suspension Points (Точки опоры на гусеницах)")]
[Tooltip("Передняя левая точка опоры")]
[SerializeField] private Transform frontLeftPoint;
[Tooltip("Передняя правая точка опоры")]
[SerializeField] private Transform frontRightPoint;
[Tooltip("Задняя левая точка опоры")]
[SerializeField] private Transform rearLeftPoint;
[Tooltip("Задняя правая точка опоры")]
[SerializeField] private Transform rearRightPoint;

[Header("Auto-Create Points (если не назначены)")]
[SerializeField] private bool autoCreatePoints = true;
[SerializeField] private float autoPointOffset = 1f;
```

**НЕ должно быть:**
```csharp
[SerializeField] private float suspensionOffset = 1f; // ❌ Этого больше нет!
```

---

## 🎯 После исправления

Когда ошибка исчезнет:

1. ✅ Выберите танк
2. ✅ В TankMovement увидите новые поля
3. ✅ Gizmos покажут точки опоры
4. ✅ Можно начать настройку!

---

**Рекомендация:** Начните с **Способа 1** (Assets → Reimport All)

*Дата: 2026-01-14*


# 🎡 Настройка вращения роликов гусениц

## ✨ Что добавлено

Ролики гусениц теперь **вращаются реалистично**:
- ✅ Вращаются с той же скоростью что и танк
- ✅ Плавное ускорение и торможение
- ✅ Правильное направление (вперед/назад)
- ✅ Синхронизация с физикой движения
- ✅ Поддержка любого количества роликов

---

## 🎮 Настройка роликов

### Шаг 1: Подготовка модели

Убедитесь что у вашего танка есть ролики гусениц:

```
Tank
├── Body
├── Turret
├── LeftTrack (левая гусеница)
│   ├── Roller_1   ← ролик
│   ├── Roller_2   ← ролик
│   ├── Roller_3   ← ролик
│   └── Roller_4   ← ролик
└── RightTrack (правая гусеница)
    ├── Roller_1   ← ролик
    ├── Roller_2   ← ролик
    ├── Roller_3   ← ролик
    └── Roller_4   ← ролик
```

**Важно:**
- Каждый ролик должен быть отдельным GameObject
- Ось вращения = локальная ось X
- Центр ролика должен быть в центре колеса

### Шаг 2: Настройка в Inspector

1. **Выберите танк** в иерархии

2. **В компоненте TankMovement** найдите секцию **Track Rollers**

3. **Настройте массивы роликов:**

   **Left Track Rollers (Левая гусеница):**
   - Нажмите на число (показывает Size)
   - Установите количество роликов (например: 4)
   - Перетащите каждый ролик левой гусеницы в поля

   **Right Track Rollers (Правая гусеница):**
   - Нажмите на число
   - Установите количество роликов (например: 4)
   - Перетащите каждый ролик правой гусеницы в поля

4. **Настройте параметры:**
   - `Roller Radius` = радиус вашего ролика (0.2-0.5)
   - `Enable Roller Rotation` = ✓

---

## ⚙️ Параметры в Inspector

### Track Rollers (Ролики гусениц)

| Параметр | Описание | Значение |
|----------|----------|----------|
| **Left Track Rollers** | Массив роликов левой гусеницы | Transform[] |
| **Right Track Rollers** | Массив роликов правой гусеницы | Transform[] |
| **Roller Radius** | Радиус ролика (для расчета скорости) | 0.2-0.5 |
| **Enable Roller Rotation** | Включить вращение | ✓ |

---

## 📐 Как определить Roller Radius?

### Способ 1: Измерить в Unity

1. Выберите любой ролик
2. В Transform посмотрите Scale
3. Если модель имеет Scale = (1, 1, 1):
   - Ролик диаметром 0.6 → Radius = 0.3
   - Ролик диаметром 0.8 → Radius = 0.4
   - Ролик диаметром 1.0 → Radius = 0.5

### Способ 2: Визуально

1. Включите Gizmos
2. Нарисуйте сферу вокруг ролика
3. Подберите радиус так чтобы сфера совпадала с роликом

### Способ 3: По модели

Если знаете реальный размер:
- Маленький ролик (30-40 см) → 0.2-0.25
- Средний ролик (60-80 см) → 0.3-0.4
- Большой ролик (100 см) → 0.5

---

## 🎯 Правильная ориентация роликов

### Проверка оси вращения

Ролики должны вращаться вокруг **локальной оси X**:

```
     Y ↑
       │
   ────┼──── X (ось вращения)
       │
       ↓ Z
```

**Как проверить:**
1. Выберите ролик
2. В Inspector найдите Rotation
3. Вручную измените X на 90
4. Ролик должен повернуться как колесо

**Если вращается неправильно:**
- Измените ось в коде (см. раздел "Настройка")
- Или поверните модель ролика в 3D редакторе

---

## 🔍 Как работает система

### Формула вращения

```csharp
// 1. Получаем текущую скорость танка
float currentSpeed = currentVelocity.magnitude;

// 2. Вычисляем угловую скорость (градусы/сек)
float rotationSpeed = (currentSpeed / (2π × radius)) × 360°

// 3. Определяем направление (вперед/назад)
direction = вперед ? положительное : отрицательное

// 4. Применяем вращение
roller.Rotate(rotationSpeed × deltaTime, 0, 0)
```

### Почему это реалистично?

**Физика:**
- Чем быстрее танк → тем быстрее вращаются ролики
- При ускорении → ролики плавно ускоряются
- При торможении → ролики плавно замедляются
- При движении назад → ролики вращаются назад

**Синхронизация:**
- Использует `currentVelocity` (текущую скорость)
- НЕ использует `targetVelocity` (целевую скорость)
- Поэтому ролики точно синхронизированы с движением!

---

## 💡 Примеры настройки

### Танк с 4 роликами на гусеницу

```
Left Track Rollers: 4
├── Element 0: LeftRoller_Front
├── Element 1: LeftRoller_Mid1
├── Element 2: LeftRoller_Mid2
└── Element 3: LeftRoller_Rear

Right Track Rollers: 4
├── Element 0: RightRoller_Front
├── Element 1: RightRoller_Mid1
├── Element 2: RightRoller_Mid2
└── Element 3: RightRoller_Rear

Roller Radius: 0.3
```

### Танк с 6 роликами на гусеницу

```
Left Track Rollers: 6
Right Track Rollers: 6
Roller Radius: 0.25
```

### Танк без роликов (только направляющие колеса)

```
Left Track Rollers: 2
├── Element 0: LeftFrontWheel
└── Element 1: LeftRearWheel

Right Track Rollers: 2
├── Element 0: RightFrontWheel
└── Element 1: RightRearWheel

Roller Radius: 0.5 (большие колеса)
```

---

## 🔧 Настройка оси вращения

Если ваши ролики вращаются вокруг другой оси:

### Для оси Y (вертикальной):

```csharp
// В методе RotateTrackRollers() замените:
roller.Rotate(rotationSpeed * Time.deltaTime, 0f, 0f, Space.Self);

// На:
roller.Rotate(0f, rotationSpeed * Time.deltaTime, 0f, Space.Self);
```

### Для оси Z (в глубину):

```csharp
roller.Rotate(0f, 0f, rotationSpeed * Time.deltaTime, Space.Self);
```

---

## 🎨 Дополнительные улучшения

### 1. Разная скорость для поворотов

Сейчас обе гусеницы вращаются одинаково. Для реализма при повороте:

```csharp
// Левая гусеница медленнее при повороте вправо
float leftSpeed = rotationSpeed * (1f - lastHorizontalInput * 0.5f);
// Правая гусеница медленнее при повороте влево
float rightSpeed = rotationSpeed * (1f + lastHorizontalInput * 0.5f);

// Применяем разные скорости
foreach (var roller in leftTrackRollers)
    roller.Rotate(leftSpeed * Time.deltaTime, 0f, 0f, Space.Self);
    
foreach (var roller in rightTrackRollers)
    roller.Rotate(rightSpeed * Time.deltaTime, 0f, 0f, Space.Self);
```

### 2. Анимация текстуры гусениц

Если у вас текстура гусениц на Material:

```csharp
[SerializeField] private Material trackMaterial;
private float textureOffset;

void RotateTrackRollers()
{
    // Сдвигаем UV текстуры
    textureOffset += currentSpeed * Time.deltaTime * 0.1f;
    trackMaterial.mainTextureOffset = new Vector2(textureOffset, 0);
}
```

### 3. Звук гусениц

```csharp
[SerializeField] private AudioSource trackSound;

void RotateTrackRollers()
{
    // Громкость и pitch зависят от скорости
    float speedPercent = currentSpeed / moveSpeed;
    trackSound.volume = speedPercent * 0.5f;
    trackSound.pitch = 0.8f + speedPercent * 0.4f;
}
```

---

## 🐛 Отладка проблем

### Ролики не вращаются

**Причина 1:** Не назначены в Inspector
**Решение:** Перетащите ролики в массивы

**Причина 2:** Enable Roller Rotation = false
**Решение:** Поставьте галочку

**Причина 3:** Танк не двигается
**Решение:** Проверьте что танк действительно движется

### Ролики вращаются неправильно

**Причина:** Неправильная ось вращения
**Решение:** 
1. Проверьте что ролик ориентирован правильно
2. Измените ось в коде (X → Y или Z)

### Ролики вращаются слишком быстро/медленно

**Причина:** Неправильный Roller Radius
**Решение:**
- Слишком быстро → увеличьте радиус
- Слишком медленно → уменьшите радиус

### Ролики вращаются в обратную сторону

**Причина:** Неправильное направление
**Решение:** Измените знак в коде:
```csharp
rotationSpeed *= -Mathf.Sign(direction); // Уберите минус
// На:
rotationSpeed *= Mathf.Sign(direction);
```

---

## ✅ Checklist настройки

- [ ] Модель танка имеет отдельные ролики
- [ ] Ролики на левой гусенице назначены
- [ ] Ролики на правой гусенице назначены
- [ ] Roller Radius установлен правильно
- [ ] Enable Roller Rotation включен
- [ ] Ролики вращаются при движении
- [ ] Направление вращения правильное
- [ ] Скорость вращения реалистична
- [ ] Плавное ускорение работает

---

## 📊 Визуальное сравнение

### До:
```
🚜 Танк движется
━━━━━━━━━━━  Гусеница (статична)
  ○  ○  ○   Ролики (не вращаются) ❌
```

### После:
```
🚜 Танк движется
━━━━━━━━━━━  Гусеница
  ⟲  ⟲  ⟲   Ролики (вращаются!) ✅
  
При ускорении: медленно → быстрее → быстро
При торможении: быстро → медленнее → стоп
```

---

## 🎉 Результат

Теперь ваш танк выглядит **максимально реалистично**:

- ✅ Ролики вращаются синхронно с движением
- ✅ Плавное ускорение и торможение
- ✅ Правильное направление вращения
- ✅ Физически точная скорость
- ✅ Как настоящий танк!

---

**Настройте ролики и наслаждайтесь реализмом!** 🎡

*Дата обновления: 2026-01-14*

